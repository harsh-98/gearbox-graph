{
  "name": "gearbox",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ gearbox",
    "create-local": "graph create --node http://localhost:8020/ gearbox",
    "remove-local": "graph remove --node http://localhost:8020/ gearbox",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 gearbox"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.23.1",
    "@graphprotocol/graph-ts": "0.23.1"
  }
}
